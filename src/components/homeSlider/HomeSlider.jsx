import SliderContainer from "./SliderContainer";
import "@/assets/homeSlider.scss";

const HomeSlider = () => {
  return (
    <section className="home_slider">
      <SliderContainer />
    </section>
  );
};

export default HomeSlider;
